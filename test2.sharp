
fn printf(str format, ...) -> i32;
fn malloc(i64 size) -> i8*;
fn free(i8* ptr);

fn main() -> i32 {
    let i32* i = malloc(sizeof(i32)) as i32*;
    *i = 2;
    let i32 a = *i;
    free(i as i8*);
    return a;
}

