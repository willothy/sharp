

fn printf(str format, ...) -> i32;
fn malloc(i64 size) -> node*;
fn free(node* ptr);

struct node {
    i32 value,
    node* next,
}

fn main() -> i32 {
    let node* ptr = malloc(sizeof(node));
    *ptr.value = 1;

    printf("ptr val: %d\n", *ptr.value);

    free(ptr);
    
    return 0;
}

