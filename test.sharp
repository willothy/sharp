

fn printf(str format, ...) -> i32;
fn malloc(i64 size) -> i32*;

struct test {
    i32 a,
    i32 b,
    i32 c
}

fn main() -> i32 {
    let i32 a = 1;
    let i32 b = 1;
    let i32 c = 2;
    let i32 d = 3;

    let i32 x = if a == 1 {
        yield if b == a {
            yield 3;
        } else {
            yield 1;
        };
    } else {
        yield 2;
    };

    printf("%d\n", x);
    return x;
}

